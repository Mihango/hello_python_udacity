# assigning tuples
dimension = 12, 14, 15
print(dimension)

# destructuring a tuple
length, width, height = dimension
print(f'length: {length} width: {width} height: {height} ')

# compare tuples
tuple_a = (1, 2)
tuple_b = 1, 2
print(f'{tuple_a == tuple_b} {tuple_a[0]}')
